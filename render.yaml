# render.yaml - Configuración para un sitio estático en Render
services:
  - type: web
    name: greencare
    runtime: static
    buildCommand: npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
